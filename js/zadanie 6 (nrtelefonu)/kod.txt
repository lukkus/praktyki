function createPhoneNumber(numbers){
  let stringNumber = numbers.toString();
  let number = stringNumber.replace(/,/g, "");
  const nawias = number.slice(0, 3);
  const trojka = number.slice(3, 6);
  const czworka = number.slice(6, 10);
  return `(${nawias}) ${trojka}-${czworka}`
}